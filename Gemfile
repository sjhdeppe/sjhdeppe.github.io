source 'https://rubygems.org'

# Matches GitHub Pages production environment
gem 'github-pages', group: :jekyll_plugins

# Standard library compatibility gems for modern Ruby
gem 'webrick'
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'mutex_m'

group :jekyll_plugins do
  gem 'jekyll-timeago', '~> 0.13.1'
end
